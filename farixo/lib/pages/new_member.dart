import 'package:flutter/material.dart';

class Newmember extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Add a new member",
      style: new TextStyle(fontSize:  20.0)),
      
 );
  }
}